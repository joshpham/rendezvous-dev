<%= form_for @phone_list do |f| %>
  <% if @phone_list.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@phone_list.errors.count, "error") %> prohibited this business from being saved:</h2>
      <ul>
      <% @phone_list.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
